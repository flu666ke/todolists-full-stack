1. in the client directory in the terminal, enter yarn
2. in the server directory in the terminal, enter yarn
3. to run the project in the server directory in the terminal, enter npm run dev

the project should run on ports 3000 for the client and 5000 for the server